# Normalize all text files to LF in the repo and on checkout — consistent line
# endings across machines and CI (overrides per-machine core.autocrlf).
* text=auto eol=lf

# Common binaries — never touch line endings.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
